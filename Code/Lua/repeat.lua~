-- print first non-empty line
repeat
   line = os.read()
until line != ''
print (line)


local sqr = x/2
repeat
   sqr = ( sqr + x/sqr) / 2
   local error = math.abs ( sqr^2 - x )
until error < x/ 10000              -- error in scope here